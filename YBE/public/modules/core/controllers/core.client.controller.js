'use strict';


angular.module('core').controller('CoreController', ['$scope','$rootScope',
    function ($scope, $rootScope) {
        /*
        * 
        */
        $rootScope.layoutState = 'default';
    }
]);